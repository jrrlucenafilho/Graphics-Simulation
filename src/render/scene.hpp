#ifndef SCENE_H
#define SCENE_H

// Módulo de renderização da cena 3D: desenha o chão, as luzes, o modelo (com
// iluminação e textura), o indicador de eixos, a curva da vitrine e a
// interface em tela. Chamado pelo callback display do GLUT.

void render_scene();

#endif
